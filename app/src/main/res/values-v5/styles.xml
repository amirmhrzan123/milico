<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="scrollbar_shape_style">
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:scrollbarStyle">insideInset</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:fadeScrollbars">false</item>
        <item name="android:fastScrollEnabled">true</item>
        <item name="android:height">500dp</item>
        <item name="android:scrollbarThumbVertical">@drawable/custom_vertical_thumb</item>
        <item name="android:scrollbarTrackVertical">@drawable/scrollbar_vertical_track</item>
        <item name="android:scrollbarSize">30dp</item>
    </style>

    <style name="scrollbar_shape_style_horizontal">
        <item name="android:scrollbarAlwaysDrawHorizontalTrack">true</item>
        <item name="android:scrollbarStyle">insideInset</item>
        <item name="android:scrollbars">horizontal</item>
        <item name="android:fadeScrollbars">false</item>
        <item name="android:fastScrollEnabled">true</item>
        <item name="android:scrollbarThumbHorizontal">@drawable/custom_horizontal_thumb</item>
        <item name="android:scrollbarTrackHorizontal">@drawable/scrollbar_vertical_track</item>
        <item name="android:scrollbarSize">30dp</item>

    </style>

    <style name="newStyle">
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:fadeScrollbars">false</item>
        <item name="android:scrollbarThumbVertical">@drawable/custom_vertical_thumb</item>
    </style>
</resources>